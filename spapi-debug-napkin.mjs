import axios from 'axios'; import { getAccessToken, generateAWSSignature } from './src/utils/auth.js'; const HOST='sellingpartnerapi-fe.amazon.com'; const MKT=process.env.SP_API_MARKETPLACE_ID; const SELLER=process.env.SP_API_SELLER_ID; async function put(token,sku,payload) { const path='/listings/2021-08-01/items/'+SELLER+'/'+sku; const qs={marketplaceIds:MKT}; const body=JSON.stringify(payload); const h=generateAWSSignature(token,'PUT',path,body,qs); const r=await axios({method:'PUT',url:'https://'+HOST+path+'?'+new URLSearchParams(qs),data:payload,headers:{host:HOST,'x-amz-access-token':token,'content-type':'application/json',...h}}); return r.data; } async function main() { const token=await getAccessToken(); const result=await put(token,'RBL-CRIMSON-NAPKIN',{ productType:'CLOTH_NAPKIN', requirements:'LISTING', attributes:{ item_name:[{value:'RB Living Cotton Dinner Napkins  Crimson, Farmhouse Dining', language_tag:'en_AU',marketplace_id:MKT}], brand:[{value:'RB Living',language_tag:'en_AU',marketplace_id:MKT}], condition_type:[{value:'new_new',marketplace_id:MKT}], parentage_level:[{value:'parent',marketplace_id:MKT}], variation_theme:[{name:'UNIT_COUNT',marketplace_id:MKT}], merchant_shipping_group:[{value:'Migrated Template',marketplace_id:MKT}] } }); console.log(JSON.stringify(result,null,2)); } main();
