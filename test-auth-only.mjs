import axios from 'axios'; import dotenv from 'dotenv'; dotenv.config(); async function run() { try { const response = await axios.post('https://api.amazon.com/auth/o2/token', { grant_type: 'refresh_token', refresh_token: process.env.SP_API_REFRESH_TOKEN, client_id: process.env.SP_API_CLIENT_ID, client_secret: process.env.SP_API_CLIENT_SECRET }, { headers: { 'Content-Type': 'application/x-www-form-urlencoded' } }); console.log('\n--- SUCCESS: LWA Auth Token Generated for MCP ---\n', response.data.access_token.substring(0, 50) + '...[active]'); console.log('\nAmazon SP-API MCP SDK Auth Verified.'); } catch (e) { console.error(e); } } run();
