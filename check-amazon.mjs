import { listingsTools } from './src/tools/listings.js'; async function run() { try { const result = await listingsTools.getListingsItem.handler({ sellerId: process.env.SP_API_SELLER_ID, sku: 'RBL-CRIMSON-TC-150' }); const data = JSON.parse(result.content[0].text); if(data.summaries) { console.log('SKU:', data.sku); console.log('Title:', data.summaries[0].itemName); console.log('Status:', data.summaries[0].status.join(', ')); } else { console.log('Response:', data); } } catch(e) { console.error('Error:', e.message); } } run();
