import fs from 'fs'; import { listingsTools } from './src/tools/listings.js'; const data = JSON.parse(fs.readFileSync('../../docs/amazon-listings-data.json', 'utf8')); const item = data.listings[0]; async function run() { try { const res = await listingsTools.putListingsItem.handler({ sellerId: process.env.SP_API_SELLER_ID, sku: item.sku, productType: item.productType, requirements: item.requirements, attributes: item.attributes }); console.log(res.content[0].text); } catch(e) { console.error('Error:', e); } } run();
