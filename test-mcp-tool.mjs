import { catalogTools } from './src/tools/catalog.js'; async function run() { try { const result = await catalogTools.searchCatalogItems.handler({ keywords: 'cotton tablecloth', includedData: ['images'] }); console.log('\n--- SUCCESS: MCP Catalog Items Acquired ---\n'); const items = JSON.parse(result.content[0].text).items; items.slice(0, 2).forEach(i => console.log('ASIN:', i.asin, 'Images:', i.images?.[0]?.images?.[0]?.link)); } catch (e) { console.error(e); } } run();
